# 🚀 Next.js 是什麼？

Next.js 是基於 **React** 的全端框架，提供：

-   檔案式路由（不用自己寫 router）
    
-   伺服器端渲染（SSR）
    
-   靜態網站生成（SSG）
    
-   API 路由（可寫後端）
    

👉 簡單說：**前端 + 後端 + 路由 + 打包都幫你做好**

----------

# ✅ 事前準備

你需要先安裝：

1.  **Node.js (建議 LTS 版本)**
    
    -   [https://nodejs.org](https://nodejs.org)
        
安裝完成後確認：

    node -v
    npm -v

----------

# 🧱 建立第一個 Next.js 專案

在終端機輸入：

    npx create-next-app@latest my-app cd my-app
    npm run dev

 
瀏覽器打開：
 `http://localhost:3000`

看到畫面就成功了 🎉

----------

# 📁 專案結構（重要）

使用 App Router（Next 13+）

    my-app/
     ├ app/
     │   ├ page.js        ← 首頁
     │   ├ layout.js      ← 共用版型
     │   └ globals.css
     ├ public/
     └ package.json

----------

# 🏠 建立首頁

`app/page.js`

    export default function Home() { 
	    return 
	    ( 
		    <div>
			    <h1>Hello Next.js</h1>
			    <p>我的第一個網站</p>
		    </div>
	    );
    }

存檔後瀏覽器會自動更新。

----------

# 📌 新增頁面（路由）

建立：

`app/about/page.js`

內容：

    export default function About() { 
	    return (<h1>About Page</h1>);
    }

打開：

`http://localhost:3000/about` 

👉 不用設定 router，Next.js 自動完成

----------

# 🔗 頁面跳轉（Link）

    import Link from "next/link";
    
    export default function Home() {
      return (
        <div>
          <h1>Home</h1>
          <Link href="/about">前往 About</Link>
        </div>
      );
    }

----------

# 🎨 使用 CSS

### 全域 CSS

    body {
      background: #f5f5f5;
    }

### 元件專屬 CSS


    .title {
      color: red;
    }

    import styles from "./home.module.css";
    
    <h1 className={styles.title}>Hello</h1>

----------

# ⚛️ 使用 React State

    "use client";
    import { useState } from "react";
    
    export default function Counter() {
      const [count, setCount] = useState(0);
    
      return (
        <div>
          <p>{count}</p>
          <button onClick={() => setCount(count + 1)}>+1</button>
        </div>
      );
    }

⚠️ 有用到互動時要加：

`"use client";` 

----------

# 🔌 建立 API（後端）

`app/api/hello/route.js` 

    export async function GET() {
      return Response.json({ message: "Hello API" });
    }

瀏覽：

`http://localhost:3000/api/hello` 

----------

# 📡 從前端呼叫 API

    "use client";
    import { useEffect, useState } from "react";
    
    export default function Page() {
      const [data, setData] = useState("");
    
      useEffect(() => {
        fetch("/api/hello")
          .then(res => res.json())
          .then(data => setData(data.message));
      }, []);
    
      return <p>{data}</p>;
    }

----------

# 📌 快速對照表

| 檔案 | 是否必須 default export |
|--|--|
| page.tsx | ✅ 是 |
| layout.tsx | ✅ 是 |
| loading.tsx | ✅ 是 |
| error.tsx | ✅ 是 |
| component.tsx | ❌ 不一定 |


# 🧠 心法口訣

👉 default export ➜ 不用 `{}`  
👉 named export ➜ 一定要 `{}`
