**Next.js 路由與多頁面管理**

這一課會讓你學會：

✔ 建立多個頁面  
✔ 使用 **Link** 進行前端跳轉（Client-side navigation）  
✔ 使用 **動態路由**（Dynamic Route）  
✔ 基本路由資料傳遞

# 🧱 目錄結構示意

    app/
     ├ page.tsx           ← 首頁
     ├ about/page.tsx     ← About 頁
     ├ todos/page.tsx     ← Todo List 頁
     └ blog/
         └ [id]/page.tsx  ← 動態路由頁


# ✅ Step 1：靜態頁面

### app/about/page.tsx

    export default function About() {
      return (
        <div>
          <h1>About Page</h1>
          <p>這是靜態頁面</p>
        </div>
      );
    }


### 路由對應

`/about → About 頁面` 

Next.js 自動對應 **檔案路徑 → URL**。

----------

# ✅ Step 2：前端跳轉（Link）

### app/page.tsx（首頁）

    import Link from "next/link";
    
    export default function Home() {
      return (
        <div>
          <h1>首頁</h1>
          <nav>
            <Link href="/about">About</Link> | 
            <Link href="/todos">Todo List</Link>
          </nav>
        </div>
      );
    }

----------

# ✅ Step 3：動態路由（Dynamic Route）

建立檔案：

`app/blog/[id]/page.tsx` 

內容：

    interface Props {
      params: { id: string };
    }
    
    export default function Blog({ params }: Props) {
      return (
        <div>
          <h1>Blog 頁面</h1>
          <p>文章 ID：{params.id}</p>
        </div>
      );
    }


----------

### 路由範例

`/blog/1 → 文章 ID: 1
/blog/abc → 文章 ID: abc` 

----------

# ✅ Step 4：從列表跳到動態頁面

    import Link from "next/link";
    
    const posts = ["react", "nextjs", "typescript"];
    
    export default function BlogList() {
      return (
        <ul>
          {posts.map((p) => (
            <li key={p}>
              <Link href={`/blog/${p}`}>{p}</Link>
            </li>
          ))}
        </ul>
      );
    }


----------

# 🧠 觀念重點

1️⃣ **檔案路徑 = URL 路由**  
2️⃣ **方括號 [id] = 動態參數**  
3️⃣ **Link** → 前端跳轉，不會整頁刷新  
4️⃣ **params** → 取得動態參數



# 1️⃣ 多個動態參數的基本寫法

假設你想要這個 URL：

`/blog/2026/02/04` 

也就是：

-   年份 `year`
    
-   月份 `month`
    
-   日期 `day`
    

### 對應目錄結構

    `app/
     └ blog/
         └ [year]/
             └ [month]/
                 └ [day]/
                     └ page.tsx` 

----------

### page.tsx 寫法

    interface Props {
      params: { year: string; month: string; day: string };
    }
    
    export default function BlogDate({ params }: Props) {
      return (
        <div>
          <h1>
            日期：{params.year}-{params.month}-{params.day}
          </h1>
        </div>
      );
    }

----------

### URL 對應 params

URL

params

/blog/2026/02/04

{ year: "2026", month: "02", day: "04" }

----------

# 2️⃣ 另一種方式：**Catch-All Routes（捕捉多個路徑段）**

如果你不想每個都一層層建立資料夾，可以用 **`[...slug]`**

`app/blog/[...slug]/page.tsx` 

### page.tsx

    interface Props {
      params: { slug: string[] }; // 會是一個陣列
    }
    
    export default function BlogSlug({ params }: Props) {
      return <p>路徑段：{params.slug.join("/")}</p>;
    }


### URL 對應結果

URL

params.slug

/blog/2026/02/04

["2026","02","04"]

/blog/nextjs/tutorial

["nextjs","tutorial"]

⚠️ 注意：`slug` 是陣列，需要用 `join` 或索引取值。

----------

# 3️⃣ 小結

1️⃣ 多個參數 → 一層一個 `[param]` 資料夾  
2️⃣ 不想層層建 → `[...slug]` catch-all route  
3️⃣ **params 對應資料夾名稱或陣列元素**


# 1️⃣ 檔案系統就是路由系統

Next.js 13+ App Router 的核心觀念：

> **檔案路徑 = 網址路由**

例如：

`app/about/page.tsx → /about
app/blog/[id]/page.tsx → /blog/:id` 

好處：

-   不需要寫 router config
    
-   URL 自動對應到對應的頁面
    
-   動態參數自動生成 `params`
    

----------

# 2️⃣ 方括號 `[param]` 的語意

方括號資料夾 / 檔案就是：

> 「這一段網址是變數」

例如：

`/blog/123 → params.id = "123"` 

如果不用資料夾，而是全部寫在同一層：

`app/blog-page.tsx` 

就 **無法自動對應 `/blog/123`**  
你要自己解析 URL，麻煩又容易錯。

----------

# 3️⃣ 為什麼層層資料夾而不是 page.tsx + query？

傳統 React Router 常用：

`<Route path="/blog/:id" element={<Blog />} />` 

Next.js 做法：

-   **URL → 檔案結構**
    
-   **檔案結構 → URL**
    

這樣做好處：

1️⃣ 所有頁面一目了然  
2️⃣ Server Component / layout / loading 都可以自動套用  
3️⃣ 動態路由天然支援多層路由

----------

# 4️⃣ Catch-All 路由 `[...slug]` 是折衷方案

如果你覺得層層資料夾太長：

`app/blog/[...slug]/page.tsx` 

-   會把多段 URL 全部放進一個陣列 `params.slug`
    
-   適合路由深度不固定的情況
    
-   少建很多資料夾，但你要自己解析陣列
    

----------

# 🟢 心法整理

-   靜態頁 → 直接 page.tsx
    
-   單一動態參數 → `[id]/page.tsx`
    
-   多個動態參數 → 一層 `[param]` 或 catch-all `[...slug]`
    
-   想簡單 → catch-all + array
    
-   想清楚、可維護 → 一層一個資料夾
    

----------

如果你願意，我可以幫你畫一個 **圖解：資料夾 → URL → params**

這樣你一眼就能理解為什麼要用 `[id]`、`[year]/[month]/[day]` 或 `[...slug]`。
