# Next.js SEO 與 Metadata 設定

✔ 設定每個頁面的 `<title>`  
✔ 設定 description、keywords  
✔ 支援 Open Graph / 社群分享  
✔ 不用額外套件，Next.js 13 App Router 內建支援

----------

# 🧱 Step 1：全域 metadata

在 `app/layout.tsx` 裡面：

    export const metadata = {
      title: "我的 Next.js App",
      description: "這是一個練習用的 Todo App",
    };
    
    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="zh-Hant">
          <body>{children}</body>
        </html>
      );
    }


-   全站的 `<title>` 與 description 都會套用這個 metadata
    

----------

# 🧱 Step 2：單頁面 metadata

在某個 page，例如 `app/about/page.tsx`：

    export const metadata = {
      title: "關於我們",
      description: "這是 About 頁面",
      openGraph: {
        title: "關於我們 - My Next App",
        description: "分享關於我們的資訊",
        url: "https://example.com/about",
        siteName: "My Next App",
        images: [
          {
            url: "/og-image.png",
            width: 1200,
            height: 630,
          },
        ],
        locale: "zh_Hant",
        type: "website",
      },
    };


Next.js 會自動在 `<head>` 加上對應 meta 標籤

----------

# 🧱 Step 3：動態 title / description

你也可以在 page.tsx 裡用 props 或 fetch 決定 title：

    interface Props {
      params: { id: string };
    }
    
    export async function generateMetadata({ params }: Props) {
      return {
        title: `文章 ${params.id}`,
        description: `這是文章 ${params.id} 的詳細頁面`,
      };
    }
   
    OR

    interface Props {
      params: { id: string };
    }
    
    export async function generateMetadata({ params }: Props) {
      const data = await fetch(`https://api.example.com/posts/${params.id}`).then(res => res.json());
      return {
        title: data.title,
        description: data.description,
      };
    }


> `generateMetadata` 會在 server 端執行，可以用 API 資料決定 SEO

----------

# 🟢 心法整理

1️⃣ **全域 metadata** → layout.tsx  
2️⃣ **單頁面 metadata** → page.tsx 的 `export const metadata`  
3️⃣ **動態 metadata** → `generateMetadata`，可以用 API


# 運作流程圖（文字版）

    [頁面請求]
          |
          v
    Next.js App Router
          |
          +--> 檢查 page.tsx 是否有 export const metadata
          |       → 如果有 → 直接插入 head |
          +--> 檢查 page.tsx 是否有 export generateMetadata
                  → 如果有 → 執行 generateMetadata() → 拿回 title/description → 插入 head
